<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="ru.pogorelov.alexey">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".ui.activities.AddNumberDriverActivity"
            android:theme="@style/AppTheme2" />
        <activity android:name=".ui.activities.AddCarNumberActivity"
            android:theme="@style/AppTheme2"/>
        <activity
            android:name=".ui.activities.SplashActivity"
            android:theme="@style/SplashBackground">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activities.InfoActivity"
            android:theme="@style/AppTheme2" />
        <activity android:name=".ui.activities.WalkthroughtContainerActivity" />
        <activity
            android:name=".ui.activities.NumberDriverLicenseActivity"
            android:windowSoftInputMode="stateVisible" />
        <activity
            android:name=".ui.activities.RegistrationNumberActivity"
            android:windowSoftInputMode="stateVisible" />
        <activity android:name=".ui.activities.MainActivity" />
    </application>

</manifest>